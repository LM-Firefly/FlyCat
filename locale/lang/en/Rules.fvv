<Rules Page>
Rules = {
    Title = "Rules"
    Summary = "View and temporarily toggle runtime rules"

    Label = {
        Proxy = "Policy"
        HitRate = "Hit rate %s"
    }

    Message = {
        ToggleFailed = "Failed to toggle rule: %s"
    }

    Empty = {
        Loading = "Loading"
        LoadingHint = "Fetching rules from the core"
        NotRunning = "Proxy is not running"
        NotRunningHint = "Start the proxy service to view rules"
        NoRules = "No rules"
        NoRulesHint = "The current config has no rules"
    }
}
